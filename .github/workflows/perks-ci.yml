name: Perks CI

on:
  push:
    paths:
      - 'Backend/services/perks/**'
      - '.github/workflows/perks-ci.yml'
  pull_request:

jobs:
  test:
    name: Go tests — perks service
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Backend/services/perks
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: Backend/services/perks/go.mod

      - name: Download dependencies
        run: go mod download

      - name: Run tests
        run: go test ./... -count=1 -short
