name: Profile CI

on:
  push:
    paths:
      - 'Backend/services/profile/**'
      - '.github/workflows/profile-ci.yml'
  pull_request:

jobs:
  test:
    name: Go tests — profile service
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Backend/services/profile
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: Backend/services/profile/go.mod

      - name: Download dependencies
        run: go mod download

      - name: Run tests
        run: go test ./... -count=1 -short
