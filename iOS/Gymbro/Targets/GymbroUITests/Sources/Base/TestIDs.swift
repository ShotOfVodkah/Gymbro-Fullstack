enum TestIDs {

    enum App {
        static let root = "app.root"
        static let mainContent = "app.main.content"
    }

    enum Auth {
        static let screen = "auth.screen"
    }

    enum Tab {
        static let workouts = "tab.workouts"
        static let feeds = "tab.feeds"
        static let profile = "tab.profile"
        static let challenges = "tab.challenges"
        static let perks = "tab.perks"
    }

    enum Screen {
        static let workouts = "screen.workouts"
        static let feeds = "screen.feeds"
        static let profile = "screen.profile"
        static let challenges = "screen.challenges"
        static let perks = "screen.perks"
    }
}
